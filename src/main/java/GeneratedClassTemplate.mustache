import org.hamcrest.Matcher;
import java.util.List;

public class {{class}} extends ValidatorBase{
    public {{class}}() { supr(); }
    public static {{class}} builder(){ return new {{class}}(); }

    @Override
    public {{class}} getThis() { return this; }


    {{#fields}}
    @SafeVarargs
    public final {{class}} {{.}}(Matcher<? super Object>... matchers){
        this.matchers.get("{{.}}").addAll(List.of(matchers));
        return this;
    }
    {{/fields}}

}